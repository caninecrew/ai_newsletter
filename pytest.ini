[pytest]
python_files = test_*.py
testpaths = tests
addopts = -v --cov=ai_newsletter --cov-report=html --cov-report=xml
markers =
    integration: marks tests that require external services
    slow: marks tests that are particularly slow
    web: marks tests for web archive features (future)